# Инструкция по запуску бота

## Шаг 1: Проверка файла .env

Убедитесь, что в папке бота есть файл `.env` с содержимым:

```
BOT_TOKEN=ваш_токен_от_BotFather
DATA_DIR=./data
TEST_MODE=True
SUBSCRIPTION_ID=
ADMIN_IDS=ваш_telegram_id
```

**Примечание:**
- `TEST_MODE=True` - тестовый режим, все пользователи имеют доступ (без подписки)
- `SUBSCRIPTION_ID` - ID подписки из BotFather (оставьте пустым пока)
- `ADMIN_IDS` - ваш Telegram ID для доступа к команде `/users` (можно узнать у бота @userinfobot)
- Кодовое слово больше не используется, доступ через подписку

## Шаг 2: Установка зависимостей

Откройте терминал в папке бота и выполните:

```bash
pip install -r requirements.txt
```

## Шаг 3: Запуск бота

### Вариант 1: Через командную строку
```bash
python bot.py
```

### Вариант 2: Через PowerShell
```powershell
cd "C:\Users\Красавчик\Documents\Бот дневник"
python bot.py
```

### Вариант 3: Двойной клик
Дважды кликните на файл `bot.py` (если Python ассоциирован с .py файлами)

## Шаг 4: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте команду `/start`
4. В тестовом режиме доступ предоставляется автоматически
5. После настройки подписок нужно будет оформить подписку для доступа

## Возможные проблемы

### Бот не отвечает на команды

1. **Проверьте, что бот запущен:**
   - В терминале должно быть сообщение "🤖 Запуск бота..."
   - Не должно быть ошибок красным цветом

2. **Проверьте токен:**
   - Токен должен быть правильным
   - Получите новый токен у @BotFather если нужно

3. **Проверьте интернет:**
   - Убедитесь, что есть подключение к интернету

4. **Перезапустите бота:**
   - Остановите бота (Ctrl+C)
   - Запустите снова

### Ошибка "ModuleNotFoundError"

Выполните:
```bash
pip install aiogram python-dotenv python-dateutil
```

### Ошибка "BOT_TOKEN не установлен"

Создайте файл `.env` в папке бота с содержимым:
```
BOT_TOKEN=ваш_токен
DATA_DIR=./data
TEST_MODE=True
SUBSCRIPTION_ID=
```

## Остановка бота

Нажмите `Ctrl+C` в терминале где запущен бот.

## Логи

Все ошибки и информация выводятся в консоль. Если бот не работает, проверьте сообщения в терминале.

